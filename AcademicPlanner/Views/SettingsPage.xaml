<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="AcademicPlanner.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Settings">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="16">
            <Label
                FontAttributes="Bold"
                FontSize="28"
                Text="Settings" />

            <Label FontSize="18" Text="PIN Unlock" />

            <Label
                x:Name="PinStatusLabel"
                FontSize="16"
                Text="PIN unlock is currently disabled." />

            <Entry
                x:Name="PinEntry"
                Keyboard="Numeric"
                MaxLength="4"
                Placeholder="Enter 4-digit PIN" />

            <Button
                x:Name="EnablePinButton"
                Clicked="OnEnablePinClicked"
                Text="Enable PIN Unlock" />

            <Button
                x:Name="DisablePinButton"
                Clicked="OnDisablePinClicked"
                Text="Disable PIN Unlock" />

            <Button Clicked="OnBackClicked" Text="Back" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>